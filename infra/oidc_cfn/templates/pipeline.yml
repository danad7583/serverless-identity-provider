AWSTemplateFormatVersion: '2010-09-09'
Description: Reusable GitHub to CodePipeline to CloudFormation deployment factory.

Parameters:
  ProjectName:
    Type: String
    Default: oidc

  EnvironmentName:
    Type: String
    Default: dev

  FoundationStackName:
    Type: String

  NetworkStackName:
    Type: String

  ApplicationStackName:
    Type: String
    Default: oidc-app-dev

  ApplicationTemplatePath:
    Type: String
    Default: infra/oidc_cfn/templates/app.yml

  BuildSpecPath:
    Type: String
    Default: infra/oidc_cfn/pipeline/buildspec.yml

  ConnectionArn:
    Type: String

  RepositoryId:
    Type: String
    Description: GitHub repo in owner/repo format.

  BranchName:
    Type: String
    Default: main

  SnapshotPrefix:
    Type: String
    Default: snapshots

  PackagedPrefix:
    Type: String
    Default: packaged

Resources:
  PipelineRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${ProjectName}-${EnvironmentName}-pipeline-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codepipeline.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: pipeline-policy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: '*'
                Resource: '*'

  CodeBuildRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${ProjectName}-${EnvironmentName}-codebuild-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: codebuild-policy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: '*'
                Resource: '*'

  CloudFormationDeployRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${ProjectName}-${EnvironmentName}-cfn-deploy-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudformation.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: cfn-deploy-policy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - cloudformation:*
                  - ec2:*
                  - lambda:*
                  - apigateway:*
                  - iam:*
                  - logs:*
                  - dynamodb:*
                  - s3:*
                  - kms:*
                  - secretsmanager:*
                Resource: '*'

  BuildProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub '${ProjectName}-${EnvironmentName}-build'
      ServiceRole: !GetAtt CodeBuildRole.Arn
      Artifacts:
        Type: CODEPIPELINE
      Source:
        Type: CODEPIPELINE
        BuildSpec: !Ref BuildSpecPath
      Environment:
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/standard:7.0
        Type: LINUX_CONTAINER
        EnvironmentVariables:
          - Name: ARTIFACT_BUCKET_NAME
            Type: PLAINTEXT
            Value:
              Fn::ImportValue: !Sub '${FoundationStackName}-ArtifactBucketName'
          - Name: APPLICATION_TEMPLATE_PATH
            Type: PLAINTEXT
            Value: !Ref ApplicationTemplatePath
          - Name: PACKAGED_TEMPLATE_PATH
            Type: PLAINTEXT
            Value: build/output/packaged-app.yml
          - Name: PACKAGED_PREFIX
            Type: PLAINTEXT
            Value: !Ref PackagedPrefix
          - Name: SNAPSHOT_PREFIX
            Type: PLAINTEXT
            Value: !Ref SnapshotPrefix
          - Name: PROJECT_NAME
            Type: PLAINTEXT
            Value: !Ref ProjectName
          - Name: ENVIRONMENT_NAME
            Type: PLAINTEXT
            Value: !Ref EnvironmentName

  Pipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      Name: !Sub '${ProjectName}-${EnvironmentName}-pipeline'
      RoleArn: !GetAtt PipelineRole.Arn
      ArtifactStore:
        Type: S3
        Location:
          Fn::ImportValue: !Sub '${FoundationStackName}-ArtifactBucketName'
        EncryptionKey:
          Id:
            Fn::ImportValue: !Sub '${FoundationStackName}-ArtifactBucketKeyArn'
          Type: KMS
      Stages:
        - Name: Source
          Actions:
            - Name: SourceFromGitHub
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: CodeStarSourceConnection
                Version: '1'
              Configuration:
                ConnectionArn: !Ref ConnectionArn
                FullRepositoryId: !Ref RepositoryId
                BranchName: !Ref BranchName
                OutputArtifactFormat: CODE_ZIP
                DetectChanges: true
              OutputArtifacts:
                - Name: SourceOutput
              RunOrder: 1

        - Name: Build
          Actions:
            - Name: BuildAndPackage
              ActionTypeId:
                Category: Build
                Owner: AWS
                Provider: CodeBuild
                Version: '1'
              Configuration:
                ProjectName: !Ref BuildProject
              InputArtifacts:
                - Name: SourceOutput
              OutputArtifacts:
                - Name: BuildOutput
              RunOrder: 1

        - Name: Deploy
          Actions:
            - Name: DeployApplication
              ActionTypeId:
                Category: Deploy
                Owner: AWS
                Provider: CloudFormation
                Version: '1'
              Configuration:
                ActionMode: CREATE_UPDATE
                RoleArn: !GetAtt CloudFormationDeployRole.Arn
                StackName: !Ref ApplicationStackName
                Capabilities: CAPABILITY_NAMED_IAM,CAPABILITY_AUTO_EXPAND
                TemplatePath: BuildOutput::build/output/packaged-app.yml
                ParameterOverrides:
                  Fn::Sub:
                    - |
                      {
                        "ProjectName": "${ProjectName}",
                        "EnvironmentName": "${EnvironmentName}",
                        "VpcId": "${VpcId}",
                        "PrivateSubnetIds": "${PrivateSubnetIds}",
                        "ExecuteApiVpcEndpointId": "${ExecuteApiVpcEndpointId}",
                        "RevocationTableName": "${RevocationTableName}",
                        "SigningKeyArn": "${SigningKeyArn}",
                        "KeyProviderSecretArn": "${KeyProviderSecretArn}",
                        "LambdaSecurityGroupId": "${LambdaSecurityGroupId}"
                      }
                    - VpcId:
                        Fn::ImportValue: !Sub '${NetworkStackName}-VpcId'
                      PrivateSubnetIds:
                        Fn::ImportValue: !Sub '${NetworkStackName}-PrivateSubnetIds'
                      ExecuteApiVpcEndpointId:
                        Fn::ImportValue: !Sub '${NetworkStackName}-ExecuteApiVpcEndpointId'
                      RevocationTableName:
                        Fn::ImportValue: !Sub '${FoundationStackName}-RevocationTableName'
                      SigningKeyArn:
                        Fn::ImportValue: !Sub '${FoundationStackName}-SigningKeyArn'
                      KeyProviderSecretArn:
                        Fn::ImportValue: !Sub '${FoundationStackName}-KeyProviderSecretArn'
                      LambdaSecurityGroupId:
                        Fn::ImportValue: !Sub '${NetworkStackName}-LambdaSecurityGroupId'
              InputArtifacts:
                - Name: BuildOutput
              RunOrder: 1
      RestartExecutionOnUpdate: true

Outputs:
  PipelineNameOutput:
    Value: !Ref Pipeline
    Export:
      Name: !Sub '${AWS::StackName}-PipelineName'

  BuildProjectNameOutput:
    Value: !Ref BuildProject
    Export:
      Name: !Sub '${AWS::StackName}-BuildProjectName'

  CloudFormationDeployRoleArnOutput:
    Value: !GetAtt CloudFormationDeployRole.Arn
    Export:
      Name: !Sub '${AWS::StackName}-CloudFormationDeployRoleArn'